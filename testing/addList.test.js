import { addTask} from '../src/modules/operations.js';

describe("Add a task", () => {
    beforeEach(() => {

    });

    test("add a task", () => {
        expect(
            addTask
        )
    })
})